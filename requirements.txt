# ブローカー接続
alpaca-py>=0.38.0
requests>=2.32.0

# LLM
anthropic>=0.49.0

# 環境変数管理
python-dotenv>=1.0.0

# データ処理（screener.py で使用）
pandas>=2.2.0
numpy>=2.0.0

# 株価取得（scripts/activist_entry_screen.py で使用）
yfinance>=0.2.50

# スケジューリング（朝8時の自動起動）
schedule>=1.2.0

# 日本の祝日判定（振替休日・浮動祝日含む）
jpholiday>=1.0.0

# J-Quants 日本株日足（ARM64 Windows で pyarrow ビルド不可のため CSV.gz 保存）
# pip install jquants-api-client --no-deps  (pandas<3.0.0 制約を回避)
jquants-api-client>=2.2.0
tenacity>=9.0.0
tqdm>=4.0.0
