# Backlog

## 来週(kabu API到着後)
- JP実弾配線: 疎通確認(トークン+1銘柄の残高/板) → KabuBroker実API化でモック前提とのズレ潰し
  → 発注せず実データで提案〜枠計算のドライラン → 少額で実発注、の順
  - 【レポート配線】CXOReportContext に jp_positions フィールドを追加し、
    run_morning_report/run_evening_report で KabuBroker.get_positions() を呼んで渡す配線を追加する。
    ※ kabu API が届いても「この配線」をしない限りレポートの JP 建玉は _DUMMY_JP（固定ダミー4銘柄）のまま。
    定義箇所: agents/cxo.py:131-157（_DUMMY_JP と jp_holdings の代入）
  - 【JP残高・配分枠】kabu 未接続中は jp_cash_jpy=0.0、allocate_budgets の JP 元金は定数¥500,000。
    kabu 接続後は get_wallet_margin()["MarginAccountWallet"] の実値を流す（main.py の既存コードで対応済み、
    ただし allocate_budgets 呼び出しで cash_usd 側と同様に usd_jpy_rate を確認すること）
- API確認: eスマートで円⇄ドル両替がAPIで可能か / 外貨決済(ドルで米株)の仕組みがあるか、
  Alpacaの日本からの入金・通貨扱い。FXRebalanceの両替設計に直結
- ヒストリカルデータ確認: kabu STATION API に過去の時系列・四本値取得エンドポイントがあるか確認。
  あれば日本株バックテストにそのまま使える。無ければ別のデータ源(日本株データAPI/リアルタイム蓄積)を検討。
  「kabuでバックテストできる」の正体(ソフトのGUI機能か、API経由か、自分で貯める前提か)も併せて確認

## レポート改善ロードマップ(段階的・フェーズ1の情報設計が固まるまで装飾に手を付けない)

### フェーズ0: メディア選定(最初に決める)
- HTMLメール/ローカルHTML/PDF/ダッシュボードのどれを本体にするか決める
- 要望(内容豊か・一目で分かる・根拠にリンク・日々見る)から「ローカルHTML本体＋メールは通知+リンクのみ」が有力。要確認

### フェーズ1: 情報設計(最優先・装飾より先)
不満の大半はここ。先に構成案を紙(or文書)で固めてから実装に入る。
- **重複排除**: 為替エージェント判定/米国株セッション前FXシグナル/夜間為替変動サマリーが
  同じことを重複記載 → 為替の話を1箇所に統合
- **配置**: CxO 方針メモを「保有銘柄の直後・リスク判断の前」に移動 → 完了（2026-06-21）
- **pre_us_fx_signal 未配線**: `EveningReportData.pre_us_fx_signal` は22:30直前の別時点FX判断を入れる設計だが、
  `CXOReportContext` に専用フィールドがなく `fx_signal` と同値が入っている。
  今回の為替統合で重複カードは削除済み。将来: `CXOReportContext` に `pre_us_fx_signal` フィールドを追加し、
  22:30直前にFXAgentを別途呼んで渡す配線をすれば別時点判断として機能する
- **欠落**: ScalpDay_JP/US・MomentSwing_JP/US の成果・ポジションを「日米×戦略」4区分で明示
- **優先順位**: 「一目で何を見るべきか」の視線の順序を設計(上から重要順)

### フェーズ2: 構造・リンク
- セクターローテーション等の判断根拠を別ページ/セクションにまとめ、本体からリンクで飛べるように
- IntelScout ダイジェスト等、深掘り情報への導線

### フェーズ3: 装飾(最後)
- 情報設計が固まった後に色・余白・フォント・チャートの見た目を整える
- 参考になるダッシュボード/運用レポートの「見せ方」を調べて取り入れる(丸コピはしない)

## FXRebalance(両替役に徹する・確定方針)
- レートAPI(get_usd_jpy()が使用可能になったので実装可)で高値掴み回避の見送りロジック
  (161円等の不利なレートでは両替を実行せず待つ。建玉の逆指値ではなく条件付き両替)
- 口座間送金の結論(再検討不要): 完全自動は規制上不可(Wise API等でも最終実行は人間の承認が必要)。
  日常の資金配分は各口座内で完結。口座間の大枠移動はシステムが提案・人間が手動実行する半自動運用とする。
- 投機/建玉/FX取引APIは不要(確定)

## 筋肉づけ(各トレーダーの腕を鍛える)
- データ供給(先) → 判断ノウハウのスキル化(後)の二段
- 武器候補: 板読み、複数時間軸ローソク足、チャートパターンと勝率、信用残、逆指値
- MomentSwing_USへの5/20日リターン追加は完了済み(一段目の実例)

### 要人発言収集強化(設計メモ・実装は別途)
目的: 論文だけでなく要人発言(半導体/AI/金融政策等)を株価反応の初動で捉える。
自分が知らない業界の要人も拾える形にしたい。

- **誰を追うか**: 個人名固定リストにしない。key_researchers.yaml と同じ思想で「役割/ポジションベースのカテゴリ」
  (主要半導体企業CEO、主要AIラボ責任者、FRB要人 等)を key_voices.yaml 的に定義。
  誰がその役割かは収集の中で動的に拾う。CXO/本人が定期見直し。完璧な網羅は目指さず育てる前提。
- **どう拾うか**: 個人SNS全監視ではなく、要人発言を報じるニュース/業界メディアをIntelScoutの収集対象に追加
  (既存のニュース収集経路の拡張)。GTC/決算/FOMC等の既知の大型イベントは日程が分かるので、
  その時間帯に収集を厚くする先回りは可能。
- **速度の限界(正直に記録)**: 報道経由なので「反応前に全部」は構造的に不可。目標は「反応の初動で気づく/乗り遅れない」。
  生中継の一言で即動く超高速勢には個人は構造的に勝てない。
- **重要度評価**: IntelScout/IntelCriticのスコアリングで「影響力の高い人物 × 保有/監視銘柄に直結するテーマ」を
  高スコアでダイジェストに上げ、無関係な発言は埋もれさせる。
- **育てる前提**: 運用しながら「この発言が実際に効いた」実績を貯め、カテゴリ・人物を更新。

### 日本の経済・市場情報収集強化(設計メモ・実装は別途)
問題意識: 現在の収集源(bioRxiv/arXiv/SEC/GitHub/StockTwits)はUS・テック・学術寄りで、
日本の経済・市場ニュースの収集が薄い。JP実弾を見据えて埋めたい。

- **発想の転換**: 日経等の有料ニュースサイトの記事全文取得はしない(規約でスクレイピング禁止・著作権上も不可)。
  「日経を取る」ではなく「日本の市場情報を、自動取得が許される経路で取る」。
- **優先は公式一次情報**(自動取得前提・市場直結・規約クリア・ノイズ少):
  - 日銀: 金融政策決定会合の結果・総裁発言 ← 前項 key_voices.yaml の JP 要人カテゴリと統合
  - 主要経済指標: GDP/CPI/日銀短観 等
  - 東証/JPX 適時開示
  日経等が報じる元ネタを直接取れば、報道を待たずに速く・規約もクリア。
- **ニュースRSS補助**: NHK/ロイター日本語等の見出し＋リンクのみ補助的に取得(本文取得はしない)。
- **限界(正直に記録)**: 一次情報で「事実」は押さえられるが「市場の解釈・センチメント」は薄い
  (なぜ上下したかの空気感はニュース解説依存で自動取得が難しい)。
- **前項との統合**: 日銀総裁は日本の金融政策要人なので key_voices.yaml に統合。
  US/テック偏重の収集を日本の経済・市場・要人にも広げる一体の IntelScout 拡張として扱う。

## MomentSwing_US バックテスト深掘り(保留)
- SL/TP感度チェック(最良値探しでなく頑健性確認・過剰最適化回避)
- 複数ポジション許可は第二段

## いずれ習得したいツール/テクニック(タスクとは別カテゴリ・出番が来たものから順次)
- **Skills(SKILL.md)**: 最優先。kabu APIの使い方や判断ノウハウ(板読み/チャートパターン勝率)をスキル化し、
  セッション間でClaude Codeに覚えさせる。来週の日本株実装で即効く。既にAlpacaスキルで使用経験あり。
- **Obsidian**: IntelScoutのダイジェスト(logs/digests/)が溜まって見返したくなったら導入。
  集めた情報を育てる場。コード実装ではなく「情報の蓄積・整理」が用途。
- **MCP**: ObsidianやLangfuse等の外部ツールをClaudeから直接触りたくなったら。
  繋ぎたい相手ができてから検討。来週の実装には不要。
- **エージェントループ**: エージェントが行動→観察→次の行動を自律で回す制御構造。
  複数エージェントの協調や自律実行を深めるときの理解対象。
- **ハーネス**: エージェントにツールを与え/ループを回し/結果を検証する足場の考え方。
  テストやバックテスト自動化の設計時に効いてくる。

## 完了済み(参考)
- v6組織リファクタ①〜⑥、市場ガード、IntelScout収集スケジュール、命名一気通貫、デッドコード掃除
- 命名積み残し(template.py等): DaytradeCandidate→ScalpDayCandidate / ValueDecision→SwingDecision 完了
- MomentSwing_US: build_us_universe() に ret_5d_pct / ret_20d_pct 追加(プロンプト要求と実入力の整合修正) 完了
- verify_cxo_reports.py: any() 形骸化チェックを廃止、カードタイトル+注入値の all() チェックに置換、サボタージュテスト追加 完了
- USD/JPY実勢レート取得: data/fx_rate.py 新設(Frankfurter API・4秒TO・24hキャッシュ・fallback表示)、
  Frankfurter 403(User-Agentブロック)修正、UA検証テスト追加、IntelScoutでキャッシュ温め追加、
  レポート表示・Critic大口判定が実勢レートを使うように 完了（2026-06-20）
- run_morning_report の allocate_budgets に usd_jpy_rate 渡し忘れ修正（155.0固定 → 実勢レート）
  完了（2026-06-20）: main.py:1472
- VIX・米10Y実勢取得: data/macro_rates.py 新設(FRED API・6秒TO・24hキャッシュ・"."欠損スキップ・fallback表示)、
  _MACRO_DATA定数廃止 → _build_macro_data()動的生成(全セッション・レポートフロー共通)。
  USD/JPYはFrankfurter据え置き(FRED DEXJPUSは実測で約7営業日遅延のため当日値が取れない)。
  完了（2026-06-20）
- レポート非対称(ScalpDay=候補/MomentSwing=決定済み)の整理: 調査の結果、戦略の時間軸の違いによる意図的な非対称。変更不要と確認(by design)。完了（2026-06-20）
